# Deploy

The frontend and backend build into two separate folders. This allows hosting them independently.

For example, the frontend can be hosted on a static host, with requests to
`/api/v1` proxied to the backend server.

For this assignment, the frontend can be deployed to GitHub Pages, and the backend to Yandex Cloud.